/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.5.3 public/models/Fish.gltf -o src/components/Fish.jsx 
*/

import React from "react";
import { useGraph } from "@react-three/fiber";
import { useGLTF, useAnimations } from "@react-three/drei";
import { SkeletonUtils } from "three-stdlib";

export default (props) => {
  const group = React.useRef();
  const { scene, animations } = useGLTF("/models/Fish.gltf");
  const clone = React.useMemo(() => SkeletonUtils.clone(scene), [scene]);
  const { nodes, materials } = useGraph(clone);
  const { actions } = useAnimations(animations, group);
  return (
    <group ref={group} {...props} dispose={null}>
      <group name="Scene">
        <group name="CharacterArmature">
          <primitive object={nodes.Body} />
          <primitive object={nodes.Head} />
          <skinnedMesh
            name="Fish_Blob"
            geometry={nodes.Fish_Blob.geometry}
            material={materials.Atlas}
            skeleton={nodes.Fish_Blob.skeleton}
          />
          <skinnedMesh
            name="Fish_Blob001"
            geometry={nodes.Fish_Blob001.geometry}
            material={materials.Atlas}
            skeleton={nodes.Fish_Blob001.skeleton}
          />
          <skinnedMesh
            name="Fish_Blob002"
            geometry={nodes.Fish_Blob002.geometry}
            material={materials.Atlas}
            skeleton={nodes.Fish_Blob002.skeleton}
          />
        </group>
      </group>
    </group>
  );
};

useGLTF.preload("/models/Fish.gltf");
