/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.5.3 public/models/Chicken.gltf -o src/components/Chicken.jsx 
*/

import React from "react";
import { useGraph } from "@react-three/fiber";
import { useGLTF, useAnimations } from "@react-three/drei";
import { SkeletonUtils } from "three-stdlib";

export default (props) => {
  const group = React.useRef();
  const { scene, animations } = useGLTF("/models/Chicken.gltf");
  const clone = React.useMemo(() => SkeletonUtils.clone(scene), [scene]);
  const { nodes, materials } = useGraph(clone);
  const { actions } = useAnimations(animations, group);
  return (
    <group ref={group} {...props} dispose={null}>
      <group name="Scene">
        <group name="CharacterArmature">
          <primitive object={nodes.Body} />
          <primitive object={nodes.Head} />
          <skinnedMesh
            name="Chicken_Blob"
            geometry={nodes.Chicken_Blob.geometry}
            material={materials.Atlas}
            skeleton={nodes.Chicken_Blob.skeleton}
          />
        </group>
      </group>
    </group>
  );
};

useGLTF.preload("/models/Chicken.gltf");
